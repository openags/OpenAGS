version: "3.9"

services:
  openags:
    build: .
    ports:
      - "19836:19836"
    volumes:
      - ~/.openags:/root/.openags
    environment:
      - OPENAGS_HOST=0.0.0.0
      - SERVER_PORT=19836
    env_file:
      - .env
    restart: unless-stopped
